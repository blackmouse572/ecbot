{
  "name": "ai",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn eccho_ai.main:app --host 0.0.0.0 --port 8000 --reload",
    "test": "uv run pytest",
    "dev:docker": "docker compose up ai --build"
  }
}
